# Site settings
title: ePay Gateway
description: > # this means to ignore newlines until "baseurl:"
  This is a short-term location for ePay markup. 
baseurl: "" # the subpath of your site, e.g. /blog/

# Build settings
markdown: kramdown
